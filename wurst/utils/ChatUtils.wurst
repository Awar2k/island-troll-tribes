package ChatUtils

public function string.isConfirmation() returns boolean
    let lower = this.toLowerCase()
    return lower == "yes" or lower == "on" or lower == "true"

public function bool.toSwitch() returns string
    return this ? "on" : "off"
